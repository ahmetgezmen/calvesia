import 'package:flutter/material.dart';

class ProvacPolicyWidget extends StatelessWidget {
  const ProvacPolicyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
